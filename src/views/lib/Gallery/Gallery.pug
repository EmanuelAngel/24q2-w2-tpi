include ../Card/Card.pug

mixin Gallery({ data = null, className = "" })
  div(class=`flex flex-col gap-8 ${className}`)
    each item in data
      +Card({ title: item.title, culture: item.culture, dynasty: item.dynasty, image: item.primaryImageSmall, className: "w-full h-full" })
    